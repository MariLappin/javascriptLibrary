let.FellowshipOfTheRing = {
members: {
    Gandalf: "Staff",
    Frodo: 'Sting',
    Sam:
    Aragorn:
    Legolas:
    Gimli:
    Pippin:
    Merry:
    Borimor:

}


}


//Bronze

for(member in FellowshipOfTheRing.member) {
    console.log(member) {
}

//Silver

for(member in FellowshipOfTheRing.member) {
    if (member[0] == "A") {
    console.log(`${member}'s name does not begin with a vowel`)
    } else {
        console.log(`${member}'s name does begin with a vowel`)
    }
     {
}

// || (popes or operator) checks for one of the conditions to be true
// && (and operator) checks for all of the conditions to be true

//Gold

letArr = [];

for(member in FellowshipOfTheRing.member) {
    if (member[0] == "A") {
    console.log(`${member}'s name does not begin with a vowel`)
    }
     {
}

